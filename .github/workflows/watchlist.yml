name: Daily Watchlist

# Pre-market, Mon-Fri. 03:15 UTC = 08:45 IST - half an hour before the open,
# so the list is on your phone while you still have time to act on it.
#
# Levels are frozen for the whole week, so this is cheap: one bulk-quote pass
# over the snapshot plus a 5m replay for the handful already above their level.
on:
  schedule:
    - cron: "15 3 * * 1-5"
  workflow_dispatch:
    inputs:
      near:
        description: "Percent below the level to count as approaching"
        type: string
        default: "3"
      explain:
        description: "How many ABOVE names to diagnose (0 = skip, faster)"
        type: string
        default: "25"

concurrency:
  group: watchlist
  cancel-in-progress: false

permissions:
  contents: read

jobs:
  watchlist:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 1

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip

      - run: pip install -r requirements.txt

      - name: Build and send the watchlist
        env:
          DHAN_CLIENT_ID: ${{ secrets.DHAN_CLIENT_ID }}
          DHAN_ACCESS_TOKEN: ${{ secrets.DHAN_ACCESS_TOKEN }}
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
        run: |
          python watchlist.py \
            --near "${{ inputs.near || '3' }}" \
            --explain "${{ inputs.explain || '25' }}"

      - name: Upload the watchlist CSV
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: watchlist
          path: "watchlist_*.csv"
          if-no-files-found: ignore
          retention-days: 30

      - name: Notify on failure
        if: failure()
        env:
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
          RUN_URL: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}
        run: |
          if [[ -z "${TELEGRAM_BOT_TOKEN}" || -z "${TELEGRAM_CHAT_ID}" ]]; then
            echo "Telegram secrets not set - skipping failure notice"
            exit 0
          fi
          MSG=$'⚠️ <b>Daily watchlist failed</b>\n'"Check the log: ${RUN_URL}"
          curl -s -X POST \
            "https://api.telegram.org/bot${TELEGRAM_BOT_TOKEN}/sendMessage" \
            --data-urlencode "chat_id=${TELEGRAM_CHAT_ID}" \
            --data-urlencode "parse_mode=HTML" \
            --data-urlencode "text=${MSG}" > /dev/null
